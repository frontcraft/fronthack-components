import React, { Component } from 'react'

/**
 * OWL Carousel 2 is a jQuery library and Fronthack supports it only for static
 * HTML version. Use another libaries for React.
 */
class OwlCarousel extends Component {
  render() {
    return(
      <p>No support for React!</p>
    )
  }
}

export default OwlCarousel
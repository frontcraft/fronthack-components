# Accordion

### Usage

`fronthack component`, then `badge`

#### JSX example:

```js
<Badge number={7} />
```

##### Properties

* `number` - number displayed in badge


#### Static HTML example:

```html
<span class="badge">3</span>
```